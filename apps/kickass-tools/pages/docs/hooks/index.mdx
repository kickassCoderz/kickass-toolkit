import { DocsLayout } from '../../../components'

[Back to Docs](/docs)

# Hooks overview docs

[useResizeObserver](/docs/hooks/use-resize-observer)

export default ({ children }) => <DocsLayout>{children}</DocsLayout>
